@page "/addcompetition/endArchers"


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Zawody oraz Łucznicy zostały dodane!</title>
    <style>
  body, html {
    height: 100%;
    margin: 0;
    font-family: Arial, sans-serif;
    display: flex;
    align-items: center;
    justify-content: center;
    background: #f4f4f4;
  }

  .confirmation-container {
    width: 90%;
    max-width: 300px;
    background: white;
    padding: 20px;
    box-shadow: 0 4px 8px rgba(0,0,0,0.1);
    border-radius: 10px;
    text-align: center;
  }
  button {
    padding: 10px 20px;
    border: none;
    color: white;
    background-color: rgb(89, 117, 55);
    border-radius: 10px;
    cursor: pointer;
    transition: background-color 0.3s;
  }

  button:hover {
    background-color: #367c39;
  }
  .confirmation-message {
    font-size: 16px;
    color: #333;
    margin-bottom: 20px;
  }
</style>
</head>
<body>
<div class="confirmation-container">
    <div class="confirmation-message">Zawody oraz Łucznicy zostały dodane!</div>
    <button onclick="location.href='/'">Wróć do menu</button>
</div>
</body>
</html>